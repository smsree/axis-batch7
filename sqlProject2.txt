create database recordmanagement
use recordmanagement
drop database recordmanagement;
CREATE TABLE USER 
(
ID INT NOT NULL AUTO_INCREMENT,
USERNAME VARCHAR(20) UNIQUE KEY,
EMAIL VARCHAR(100),
PASSWORD VARCHAR(256), 
PRIMARY KEY (ID),
UNIQUE KEY (EMAIL)
);
select * from user;
CREATE TABLE ROLE 
(
ID INT NOT NULL AUTO_INCREMENT,
NAME VARCHAR(20),
PRIMARY KEY (ID)
);

CREATE TABLE USER_ROLE(
USER_ID int,
ROLE_ID int,
FOREIGN KEY (user_id)
REFERENCES user(id),
FOREIGN KEY (role_id)
REFERENCES role(id)
);
DROP TABLE REPORTMODEL
CREATE TABLE REPORTMODEL(
ID INT NOT NULL auto_increment  PRIMARY KEY,
USERNAME VARCHAR(20) UNIQUE KEY,
EMAIL VARCHAR(20),
DOWNLOADEDITEMS VARCHAR(120),
TIMESTAMP VARCHAR(30)
);
select * from user_role;
INSERT INTO ROLE(NAME) VALUES("ROLE_OPERATIONAL"); 
INSERT INTO ROLE(NAME) VALUES("ROLE_USER");
INSERT INTO ROLE(NAME) VALUES("ROLE_ADMIN");
INSERT INTO ROLE(NAME) VALUES("ROLE_AUDIT");
-- ,("ROLE_ADMIN"),("ROLE_AUDIT"),("ROLE_OPERATIONAL");
SELECT * FROM ROLE;
select * from user;

select * from report_model;
